import type { AppContext } from './context';
import type { LoaderList } from './loader';
declare class App {
    private readonly loaders;
    private readonly components;
    private readonly eventStore;
    readonly context: AppContext;
    constructor(components?: LoaderList);
    private static createEventStore;
    private static createHook;
    private getComponentContext;
    private createComponent;
    add(components?: LoaderList): void;
    mount(root?: HTMLElement | Document): void;
}
export default App;
//# sourceMappingURL=app.d.ts.map